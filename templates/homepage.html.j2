<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Alastair's Blog Homepage</title>
    <script type="text/javascript" async
      src="https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.7/MathJax.js?config=TeX-MML-AM_CHTML">
    </script>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 60%;
            margin: 20px auto;
            background: white;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .witty-quote {
            font-style: italic;
            margin: 10px 0 5px 0;
            color: #555;
        }
        .quote-attribution {
            text-align: right;
            font-size: 0.9em;
            color: #777;
            margin-bottom: 20px;
        }
        .post-list {
            list-style: none;
            padding: 0;
        }
        .post-list li {
            margin-bottom: 10px;
            border-bottom: 1px solid #ddd;
            padding-bottom: 10px;
        }
        .post-list li a {
            font-size: 1.2em;
            text-decoration: none;
            color: #333;
        }
        .post-meta {
            color: #777;
            font-size: 0.9em;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Alastair's Blog</h1>
        {% if quote %}
        <blockquote class="witty-quote">
            {{ quote | safe }}
        </blockquote>
        <div class="quote-attribution">
            — ChatGPT, {{ build_date }}
        </div>
        {% endif %}
        <ul class="post-list">
            {% for post in posts %}
            <li>
                <a href="build/{{ post.filename }}">{{ post.title }}</a>
                <br>
                <small class="post-meta">by {{ post.author }} on {{ post.date }}</small>
            </li>
            {% endfor %}
        </ul>
    </div>
</body>
</html>
